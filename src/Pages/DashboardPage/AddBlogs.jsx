import CustomTitle from "../../Hooks/CustomTitle";

const AddBlogs = () => {
    return (
        <div>
            <CustomTitle
            subHeader="new blogs"
            header="Add New Forum"
            ></CustomTitle>
        </div>
    );
};

export default AddBlogs;